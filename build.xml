<?xml version="1.0" encoding="UTF-8"?>
<project name="Reverse Shell Receiver" default="jar" basedir=".">
    <property name="src.dir" value="src/main/java"/>
    <property name="build.dir" value="build"/>
    <property name="dist.dir" value="dist"/>
    <property name="lib.dir" value="../Tripod/lib"/>
    <property name="jar.name" value="Reverse_Shell_Receiver.jar"/>

    <path id="classpath">
        <fileset dir="${lib.dir}" includes="burp.jar"/>
    </path>

    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <target name="compile" depends="clean">
        <mkdir dir="${build.dir}"/>
        <javac srcdir="${src.dir}" destdir="${build.dir}" classpathref="classpath" includeantruntime="false" source="1.8" target="1.8"/>
    </target>

    <target name="jar" depends="compile">
        <mkdir dir="${dist.dir}"/>
        <jar destfile="${dist.dir}/${jar.name}" basedir="${build.dir}">
            <manifest>
                <attribute name="Main-Class" value="burp.BurpExtender"/>
            </manifest>
        </jar>
    </target>
</project>
